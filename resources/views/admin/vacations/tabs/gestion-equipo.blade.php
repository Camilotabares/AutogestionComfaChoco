<x-wire-card>
    <x-slot name="title">
        <div class="flex items-center gap-2 text-primary-600 font-semibold">
            <i class="fa-solid fa-people-group"></i>
            {{ __('Gestión de Vacaciones del Equipo') }}
        </div>
    </x-slot>

    <div class="bg-gray-50 border border-gray-200 rounded-lg p-6 text-gray-600">
        {{ __('Esta sección permitirá gestionar las solicitudes del equipo una vez se definan los permisos y flujos correspondientes.') }}
    </div>
</x-wire-card>
